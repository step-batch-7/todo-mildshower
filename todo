--done-- write a todo
--done-- do a npm init
--done-- make folder structure
--done-- add server and controller(app) components
--done-- make a primitive structure to take data from web and show
--done-- making html responsibility should be transferred to frontEnd
--done-- make done/undone feature of todo tasks
--done-- make it enable to fetch previous to-dos and show them
--done-- dynamic "remaining task count"
--done-- introduce todoListCollection class
--done-- introduce task class
--done-- add feature to remove particular task
--done-- add feature to remove todo from list
--done-- add feature to add more task to a pre-made todo
--done-- refactor index.js and separate functions in modules
--done-- make todoList title editable
--done-- make task items editable
--done-- implement todoList search feature
--done-- implement task search feature
--done-- design a login page
--done-- design a sign up page
--done-- implement login feature
--done---- use cookie implantation and session classes
--done---- use user class to hold user context
--done---- use cookies to determine session and ask for login if not logged in
-- make logout feature
-- directly login when signing up
...